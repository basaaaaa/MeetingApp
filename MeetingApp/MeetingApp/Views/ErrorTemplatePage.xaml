<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="http://prismlibrary.com"
             x:Class="MeetingApp.Views.ErrorTemplatePage"
             Title="{Binding ErrorMessageTitle}">

    <StackLayout Margin="5,5,5,5">
        <Label TextColor="Red" Text="{Binding ErrorMessage}" FontSize="30" FontAttributes="Bold" HorizontalOptions="Center"/>
        <Label Text="{Binding ErrorMessageDespription}" HorizontalOptions="Center"/>
        <Button HorizontalOptions="Center" VerticalOptions="Center" Text="戻る" Command="{Binding NavigateLoginPageCommand}" BackgroundColor="Black" TextColor="White"/>
    </StackLayout>
  
</ContentPage>